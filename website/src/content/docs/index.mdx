---
title: dysonsphere
description: "Altair themes, chart utilities, palettes, statistical annotations, and metadata-aware exports."
tableOfContents: false
hero:
  image:
    file: ../../assets/dysonsphere_logo.svg
  tagline: >-
    Altair themes, palettes, statistical annotations, and chart exports.
  actions:
    # Relative links (resolved from the "/" or "/dysonsphere/" landing URL): frontmatter can't
    # go through the remark base-prefix plugin, and Starlight passes these hrefs through as-is.
    - text: Get started
      link: guides/getting-started/
      icon: right-arrow
    - text: Plot your data
      link: studio/
      variant: minimal
    - text: GitHub
      link: https://github.com/dkkung/dysonsphere
      icon: external
      variant: minimal
---

import { Card, CardGrid, Code } from '@astrojs/starlight/components';
import Chart from '../../components/Chart.astro';
import HeroTypewriter from '../../components/HeroTypewriter.astro';
// The raw source of the hero example, so the shown code and the rendered chart come from one file.
import homeSource from '../../../examples/home_demo.py?raw';

<HeroTypewriter />

<div class="landing">

<section class="landing__install">
```sh
pip install dysonsphere
```
</section>

<section class="landing__demo">
	<div class="landing__demo-code">
		<Code code={homeSource.trimEnd()} lang="python" />
	</div>
	<div class="landing__demo-chart">
		<Chart name="home_demo" alt="Strip plot of fuel efficiency by car origin with significance brackets" />
	</div>
</section>

<section class="landing__features">
<CardGrid>
	<Card title="Consistent defaults" icon="approve-check">
		Charts use consistent typography, tick geometry, and mark styling. SVG output uses the same settings.
	</Card>
	<Card title="Perceptually uniform palettes" icon="star">
		Dysonsphere's native palettes use Oklab interpolation and arc-length resampling. Imported palettes may use different construction rules. Export palettes as Illustrator swatches.
	</Card>
	<Card title="Statistical annotations" icon="magnifier">
		Compute omnibus tests, post-hoc comparisons, and correlations with SciPy. Add the results as Altair layers with <code>+</code> and export exact statistics.
	</Card>
	<Card title="Composable marks & layers" icon="add-document">
		Add violins, strip plots, reference lines, text, shading, point labels, multilabels, and log or power minor ticks as Altair layers.
	</Card>
	<Card title="Self-describing exports" icon="download">
		<code>save()</code> writes metadata to SVG, PNG, and JSON. JSON exports also contain captured chart data and can be loaded with <code>load()</code>; <code>read()</code> reads metadata from the supported formats.
	</Card>
	<Card title="Runs in your browser" icon="rocket">
		{/* Relative hrefs (this page renders at the site root, "/" or "/dysonsphere/"): raw JSX
		    anchors bypass the remark base-prefix plugin, so root-relative ones would 404 when
		    deployed under a base path. */}
		The <a href="studio/">Chart Studio</a> runs the library in WebAssembly. Upload data or edit Python in the browser without installing Python.
	</Card>
</CardGrid>
</section>

</div>
