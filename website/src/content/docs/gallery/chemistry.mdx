---
title: Structure & chemistry
description: Orbitals, diffraction, protein dihedrals, spectra, and reaction energetics.
---

import Example from '../../../components/Example.astro';

Examples cover structure, spectroscopy, and reaction energetics. Select **`</> code`** to view a figure's source or **Open in studio** to edit it.

<Example name="orbital" codeToggle caption="Signed 3d atomic-orbital cross-section with four alternating-sign lobes on the <code>pinksblues</code> diverging scale." alt="Atomic orbital wavefunction cross-section" />

<Example name="diffraction" codeToggle caption="Electron diffraction pattern with a hexagonal reciprocal lattice of Bragg spots in monochrome <code>eclipse</code>." alt="Electron diffraction pattern" />

<Example name="ramachandran" codeToggle caption="Ramachandran plot of backbone φ/ψ dihedral density, showing α-helix and β-sheet basins." alt="Ramachandran dihedral density plot" />

<Example name="mass_spectrum" codeToggle caption="Mass spectrum with an isotope envelope and fragment peaks labeled with <code>ds.text</code>." alt="Mass spectrum stick plot with labeled peaks" />

<Example name="reaction_energy" codeToggle caption="Reaction free-energy profile with transition states and the intermediate annotated with <code>ds.text</code>." alt="Reaction free-energy profile" />

<Example name="titration" codeToggle caption="Weak-acid/strong-base titration curve with the equivalence point and pKa marked with <code>ds.rule</code>." alt="Acid-base titration curve" />

<Example name="phase_diagram" codeToggle caption="CO₂ pressure-temperature phase diagram with boundaries, triple and critical points, and phase labels added with <code>ds.text</code>." alt="CO2 pressure-temperature phase diagram" />
