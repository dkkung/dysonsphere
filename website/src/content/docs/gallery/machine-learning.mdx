---
title: Machine learning
description: Embeddings, decision surfaces, evaluation curves, learning curves, and model interpretability.
---

import Example from '../../../components/Example.astro';

Embeddings, classifiers, evaluation, training, and interpretability. Press **`</> code`** on any
figure to read the code that made it, or **Open in studio** to run and edit it live.

<Example name="umap" codeToggle caption="A single-cell UMAP embedding - clusters in the categorical palette, each labeled at its centroid with <code>add_labels</code>." alt="Single-cell UMAP cluster embedding" />

<Example name="decision_surface" codeToggle caption="A classifier's decision surface - a kernel probability field behind the two-moons data." alt="Classifier decision boundary over two-moons data" />

<Example name="confusion_matrix" codeToggle caption="A confusion matrix - cell counts placed on the band scale with <code>add_text</code>." alt="Confusion matrix heatmap with counts" />

<Example name="roc_curve" codeToggle caption="ROC curves for three classifiers - true vs false positive rate with AUC in the legend and the chance diagonal." alt="ROC curves for three classifiers" />

<Example name="learning_curves" codeToggle caption="Learning curves - training vs validation loss and accuracy over epochs, the validation loss turning up as the model begins to overfit." alt="Training and validation learning curves" />

<Example name="shap_beeswarm" codeToggle caption="A SHAP summary beeswarm - per-feature attribution swarms (density-scaled by <code>add_quasirandom</code>) coloured by feature value, features ranked by importance." alt="SHAP feature-attribution beeswarm plot" />

<Example name="streamgraph" codeToggle caption="A streamgraph - ML framework share of published models across a decade, a centre-stacked <code>mark_area</code>." alt="Streamgraph of ML framework adoption over time" />
