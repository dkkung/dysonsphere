---
title: Machine learning
description: Decision surfaces, evaluation curves, learning curves, and model interpretability.
---

import Example from '../../../components/Example.astro';

Examples cover classifiers, evaluation, training, and interpretability. Select **`</> code`** to view a figure's source or **Open in studio** to edit it.

<Example name="decision_surface" codeToggle caption="Classifier decision surface with a kernel probability field behind the two-moons data." alt="Classifier decision boundary over two-moons data" />

<Example name="confusion_matrix" codeToggle caption="Confusion matrix with cell counts on the band scale, annotated with <code>ds.text</code>." alt="Confusion matrix heatmap with counts" />

<Example name="roc_curve" codeToggle caption="ROC curves for three classifiers, plotting true-positive rate against false-positive rate; the legend includes AUC values and the chart includes a chance diagonal." alt="ROC curves for three classifiers" />

<Example name="learning_curves" codeToggle caption="Training and validation loss and accuracy over epochs; validation loss rises as the model begins to overfit." alt="Training and validation learning curves" />

<Example name="shap_beeswarm" codeToggle caption="SHAP summary beeswarm of per-feature attributions. Points are positioned using density-scaled <code>ds.transforms.quasirandom</code> and colored by feature value; features are ranked by importance." alt="SHAP feature-attribution beeswarm plot" />

<Example name="streamgraph" codeToggle caption="Streamgraph of machine-learning framework shares among published models over a decade, using center-stacked <code>mark_area</code>." alt="Streamgraph of ML framework adoption over time" />
