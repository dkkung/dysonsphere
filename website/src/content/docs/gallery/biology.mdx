---
title: Molecular biology
description: Differential expression, survival, dose-response, and genome-wide association.
---

import Example from '../../../components/Example.astro';

Examples include genomics, transcriptomics, single-cell data, and pharmacology. Select **`</> code`** to view a figure's source or **Open in studio** to edit it.

<Example name="volcano" codeToggle caption="Differential-expression volcano plot of log2 fold change vs. −log10 p, with top hits labeled automatically. Uses <code>ds.biology.volcano</code> from the biology extension." alt="Differential-expression volcano plot with labeled genes" />

<Example name="ma_plot" codeToggle caption="MA plot of RNA-seq log2 fold change vs. mean expression, with up- and down-regulated genes colored separately and genes labeled by <code>ds.labels</code>." alt="RNA-seq MA plot with labeled genes" />

<Example name="table_qc" codeToggle caption="Sequencing QC summary table using <code>mark_table</code>, with read counts in SI notation, per-metric formatting, and mapping rate shaded green by value." alt="Sequencing QC summary table with a value-shaded mapping-rate column" />

<Example name="expression_heatmap" codeToggle caption="A gene-expression heatmap - z-scored RNA-seq across control and treated samples, genes ordered by program (induced, repressed, housekeeping), on the <code>redsblues</code> diverging scale." alt="Gene-expression heatmap of z-scored RNA-seq" />

<Example name="survival_km" codeToggle caption="Kaplan-Meier survival curves for treatment and control, with step estimates and a tick at each censored subject." alt="Kaplan-Meier survival curves with censoring ticks" />

<Example name="dose_response" codeToggle caption="Dose-response curves of viability vs. log dose for three compounds, with triplicate means ± SD and sigmoid IC₅₀ fits." alt="Dose-response curves with IC50 fits" />

<Example name="manhattan" codeToggle caption="GWAS Manhattan plot with alternating chromosome colors, a genome-wide significance threshold marked with <code>ds.rule</code>, and top hits labeled by <code>ds.labels</code>." alt="GWAS Manhattan plot with labeled hits" />

<Example name="qpcr" codeToggle caption="Grouped qPCR bars of cytokine expression (vehicle vs. LPS), with SEM error bars and a within-gene significance bracket for each gene from <code>ds.stats.comparisons(xOffset=...)</code>. The GAPDH test is ns; GAPDH is the housekeeping control." alt="qPCR grouped bar chart with per-gene significance brackets" />

<Example name="umap" codeToggle caption="Single-cell UMAP embedding of PBMC clusters in the categorical palette, with each cluster labeled at its centroid by <code>ds.labels</code>." alt="Single-cell UMAP cluster embedding" />

<Example name="celltype_donut" codeToggle caption="PBMC cell-type fractions in a <code>mark_arc</code> donut, with the total cell count at the center (arcs are donuts by default under the theme)." alt="Cell-type composition donut chart" />
