---
title: Molecular biology
description: Differential expression, survival, dose-response, and genome-wide association.
---

import Example from '../../../components/Example.astro';

Genomics, transcriptomics, and pharmacology. Press **`</> code`** on any figure to read the code
that made it, or **Open in studio** to run and edit it live.

<Example name="volcano" codeToggle caption="A volcano plot - differential gene expression (log2 fold change vs −log10 p), the top hits auto-labeled. Built on <code>ds.biology.volcano</code> from the biology extension." alt="Differential-expression volcano plot with labeled genes" />

<Example name="table_qc" codeToggle caption="A sequencing QC summary as a <code>mark_table</code> - read counts in SI notation, per-metric formatting, and the mapping rate shaded green by value (a quality heatmap column)." alt="Sequencing QC summary table with a value-shaded mapping-rate column" />

<Example name="expression_heatmap" codeToggle caption="A gene-expression heatmap - z-scored RNA-seq across control and treated samples, genes ordered by program (induced, repressed, housekeeping), on the <code>redsblues</code> diverging scale." alt="Gene-expression heatmap of z-scored RNA-seq" />

<Example name="survival_km" codeToggle caption="Kaplan-Meier survival curves - treatment vs control step estimates with a tick at each censored subject." alt="Kaplan-Meier survival curves with censoring ticks" />

<Example name="dose_response" codeToggle caption="Dose-response curves - viability vs log dose for three compounds, mean ± SD of the triplicate with the sigmoid (IC₅₀) fits." alt="Dose-response curves with IC50 fits" />

<Example name="manhattan" codeToggle caption="A GWAS Manhattan plot - alternating chromosome colors, a genome-wide significance <code>add_rule</code>, and top hits labeled by <code>add_labels</code>." alt="GWAS Manhattan plot with labeled hits" />
