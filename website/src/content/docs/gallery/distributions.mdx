---
title: Distributions & statistics
description: Rainclouds, ridgelines, and empirical distributions.
---

import Example from '../../../components/Example.astro';

Distributions and statistical annotations. Press **`</> code`** on any figure to read the code that
made it, or **Open in studio** to run and edit it live.

<Example name="raincloud" codeToggle caption="A raincloud - <code>mark_violin</code> (KDE + boxplot) over jittered raw points, across three conditions." alt="Raincloud plot of three conditions" />

<Example name="ridgeline" codeToggle caption="A ridgeline plot - stacked kernel densities across the year, in the categorical palette." alt="Ridgeline plot of monthly distributions" />


<Example name="expression_comparison" codeToggle caption="Group comparison - a beeswarm strip with pairwise Mann-Whitney brackets from <code>add_comparisons</code>." alt="Beeswarm strip with p-value brackets" />

<Example name="ecdf" codeToggle caption="Empirical cumulative distributions - step curves for three groups in the categorical palette." alt="Empirical cumulative distribution curves" />

<Example name="qq_plot" codeToggle caption="A normal Q-Q plot - a right-skewed sample departing from the y = x reference line." alt="Normal quantile-quantile plot" />
