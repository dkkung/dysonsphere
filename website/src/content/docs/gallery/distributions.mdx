---
title: Distributions & statistics
description: Violins, ridgelines, and empirical distributions.
---

import Example from '../../../components/Example.astro';

Examples cover distributions and statistical annotations. Select **`</> code`** to view a figure's source or **Open in studio** to edit it.

<Example name="violin_sample_size" codeToggle caption="Trimmed quartile violins across three conditions, with per-group sample sizes from <code>add_multilabel</code>." alt="Violin plots of three conditions with sample sizes below the axis" />

<Example name="ridgeline" codeToggle caption="Ridgeline plot of monthly kernel densities in the categorical palette." alt="Ridgeline plot of monthly distributions" />


<Example name="expression_comparison" codeToggle caption="Group comparison with a beeswarm strip and pairwise Mann-Whitney brackets from <code>ds.stats.comparisons</code>." alt="Beeswarm strip with p-value brackets" />

<Example name="ecdf" codeToggle caption="Empirical cumulative distribution curves for three groups, shown as steps in the categorical palette." alt="Empirical cumulative distribution curves" />

<Example name="qq_plot" codeToggle caption="Normal Q-Q plot showing a right-skewed sample's departure from the y = x reference line." alt="Normal quantile-quantile plot" />

<Example name="correlation_matrix" codeToggle caption="Feature correlation matrix with cell labels for Pearson r on the <code>redsblues</code> diverging scale." alt="Feature correlation matrix heatmap" />

<Example name="correlation_groups" codeToggle caption="Correlation by series for biomarker vs. drug response across three cell lines. Each line shows a fit, confidence interval band, and <code>r</code> from <code>ds.stats.correlation(groupBy=...)</code>. Fit lines are colored to match their points." alt="Per-cell-line correlation with fit lines and coefficients" />
