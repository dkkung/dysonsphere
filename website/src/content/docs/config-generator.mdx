---
title: Config generator
description: "Edit a dysonsphere.toml template with a reference for theme() parameters."
tableOfContents: false
---

import ConfigGenerator from '../../components/ConfigGenerator.astro';

The editor starts with the file produced by `ds.create_config()`. The cheat sheet lists every `ds.theme()` parameter and its built-in default. Select a row to insert it, edit the value, and copy or download the result. See
[Global theme overrides](/guides/configuration/) for where the file lives and how sections merge.

<ConfigGenerator />
