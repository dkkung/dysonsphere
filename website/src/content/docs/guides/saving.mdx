---
title: Saving & reading
description: Export self-describing figures and read the provenance, statistics, and data back out.
---

`ds.save()` is more than a renderer: it embeds provenance, the resolved theme, any statistics, and
your data into the exported files, so every figure is its own lab-notebook entry. `ds.read()` and
`ds.load()` pull that information back.

## Saving

```python
import dysonsphere as ds

ds.theme()
# ... build `chart` ...

ds.save(chart, "figure")
```

By default this writes `figure.svg` and `figure.json`. Control the outputs with `format=` (any of
`"svg"`, `"png"`, `"json"`, `"html"` - a string or a list) and `background=` (`"light"`, `"dark"`,
or both). A two-background export gets `_light` / `_dark` suffixes; a single background gets clean
names.

```python
ds.save(chart, "figure", format=["svg", "png", "json"], background=["light", "dark"])
ds.save(chart, "figure", format="html")   # a self-contained interactive HTML page
```

The SVG post-processing means a notebook's inline preview (Altair renders straight from Vega-Lite)
can differ subtly from the saved file. `ds.show(chart)` renders a chart *through* the `save()`
pipeline so the preview matches the exported figure exactly.

The SVG is post-processed for publication: `<g>` wrappers are flattened for Illustrator, axis ticks
and log/power minor ticks are snapped to exact positions, and superscript exponents are realigned.

## What gets embedded

When `saveMetadata=True` (the default), `save()` writes a `dysonsphere` metadata block into all
three formats:

- **provenance** - a content checksum, an export identifier, the user, the timestamp, and the
  versions of Python, Altair, dysonsphere, numpy, scipy, and polars.
- **theme** - the resolved `ds.theme()` arguments, so the look can be reproduced exactly.
- **statistics** - the structured records from any `add_comparisons()` / `add_correlation()` on the
  chart (exact p-values, effect sizes, descriptives).
- **report** - the human-readable version of those statistics.

## What the embedded metadata looks like

This is not hand-waving - here is the actual block from a real save. Take a strip plot with a
Kruskal-Wallis comparison on the classic `cars` dataset:

```python
chart = ds.mark_strip(cars, "Origin", "Miles_per_Gallon", origins) + ds.add_comparisons(
    cars, "Origin", "Miles_per_Gallon",
    [("USA", "Europe"), ("USA", "Japan")],
    test="kruskal", omnibusVerbose=True, categories=origins,
)
ds.save(chart, "mpg_by_origin", format="json")
```

`ds.read("mpg_by_origin.json", what="metadata")` returns a `dict` with four keys - `provenance`,
`statistics`, `theme`, and `report`. The **provenance** records who/when/what and pins every
dependency version, so the figure is reproducible:

```json
{
  "dataChecksum": ["sha256:dcd8d84cfe3b6e5cbcdaca8e43bfd44ef3f466407b862f9f03034bae27da5d4d"],
  "vegaliteChecksum": "sha256:93014850ee942b65de6b679b18be05aa195bc2d3ee3617f5ad0a9aec5b1ea519",
  "exportIdentifier": "53ab9835-016b-4600-8ceb-1065cb341417",
  "user": "douglaskung",
  "script": "-",
  "timestamp": "2026-07-06T07:25:24Z",
  "environment": {
    "python": "3.11.8", "altair": "6.2.1", "dysonsphere": "3.0.0",
    "numpy": "2.4.6", "scipy": "1.17.1", "polars": "1.41.2"
  }
}
```

The three checksums answer different questions: `dataChecksum` (one per dataframe,
order-independent) is the **data** identity - two charts of the same numbers match even if drawn
differently; `vegaliteChecksum` is the exact **spec content**; `exportIdentifier` is shared by
every file from one `save()` call.

The **statistics** entry is a list of structured records - exact p-values and effect sizes, not
the rounded on-plot labels. One omnibus record from the chart above (trimmed):

```json
{
  "kind": "omnibus",
  "test": "kruskal",
  "groups": [
    {"label": "USA", "n": 245, "mean": 20.03, "sd": 6.44, "median": 18.5, "q1": 15.0, "q3": 24.0},
    {"label": "Europe", "n": 68, "mean": 27.60, "sd": 6.58, "median": 26.0, "q1": 23.75, "q3": 30.13},
    {"label": "Japan", "n": 79, "mean": 30.45, "sd": 6.09, "median": 31.6, "q1": 25.7, "q3": 34.05}
  ],
  "omnibus": {
    "name": "Kruskal-Wallis",
    "statistic": {"symbol": "H", "value": 132.09, "df": [2]},
    "pvalue": 2.078e-29,
    "effect": {"name": "epsilon_squared", "symbol": "ε²", "value": 0.338}
  },
  "comparisons": {
    "test": "dunn", "correction": null,
    "pairs": [
      {"group1": "USA", "group2": "Europe", "pvalue": 3.99e-13, "effect": {"symbol": "r", "value": 0.606}},
      {"group1": "USA", "group2": "Japan",  "pvalue": 4.47e-25, "effect": {"symbol": "r", "value": 0.746}}
    ]
  }
}
```

And the **report** key is that same content rendered as the plain-text lab note you get from
`ds.read("mpg_by_origin.json")` (the default `what="report"`):

```
Statistics | Omnibus | Kruskal-Wallis
─────────────────────────────────────

H(2) = 132, P = 2.08e-29
Effect size: ε² = 0.338

Group descriptives:
  USA     n=245  mean=20  sd=6.44  median=18.5  IQR=[15, 24]  range=[9, 39]
  Europe  n=68   mean=27.6  sd=6.58  median=26  IQR=[23.8, 30.1]  range=[16.2, 44.3]
  Japan   n=79   mean=30.5  sd=6.09  median=31.6  IQR=[25.7, 34]  range=[18, 46.6]

Post-hoc (dunn):
  USA vs Europe    P = 3.99e-13  r = 0.606
  USA vs Japan     P = 4.47e-25  r = 0.746
  Europe vs Japan  P = 0.0377  r = 0.297

Provenance
──────────

Generated by douglaskung with - on 2026-07-06T07:25:24Z using Python 3.11.8,
Altair 6.2.1, dysonsphere 3.0.0, NumPy 2.4.6, SciPy 1.17.1, Polars 1.41.2.
Data checksum: sha256:dcd8d84c…
Vega-Lite spec checksum: sha256:93014850…
Export identifier: 53ab9835-016b-4600-8ceb-1065cb341417
```

The **data** itself round-trips too. `ds.read("mpg_by_origin.json", what="data")` returns the
original 392-row dataframe - every column Altair inlined, including transform columns like
`jitter_x` (the honest data of record), with dysonsphere's own sidecar layers (bracket
coordinates, error bars) filtered out.

## Reading it back

```python
ds.read("figure.json")                    # the human-readable report (default)
ds.read("figure.json", what="statistics") # structured records (list of dicts)
ds.read("figure.json", what="metadata")   # the whole dysonsphere block (shown above)
ds.read("figure.json", what="data")       # the original DataFrame
```

`read()` works on JSON, SVG, and PNG for metadata and reports; `what="data"` is JSON-only (only the
JSON carries the full data). `what="data"` returns the user's frame, filtering out dysonsphere's
internal sidecar datasets; pick the output type with `output=` (`"polars"`, `"pandas"`, `"duckdb"`,
or `"records"`).

## Rebuilding the chart

```python
chart = ds.load("figure.json")            # an Altair object, theme re-applied
spec  = ds.load("figure.json", raw=True)  # the untouched Vega-Lite spec dict
```

`load()` is JSON-only (it needs the full spec) and re-applies the baked theme so the reconstructed
chart renders identically.

## Statistics without a file

`add_comparisons()` and `add_correlation()` can surface their report directly:

```python
chart = base + ds.add_comparisons(df, "group", "value", pairs, test="anova", report=True)
# report=True prints it; save="dir/" writes a timestamped .txt
```

See the [Saving & loading](/reference/export/) and [Reading exports](/reference/metadata/)
reference pages for the full API.
