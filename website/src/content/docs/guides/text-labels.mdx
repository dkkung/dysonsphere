---
title: Text & labels
description: Text annotations with ds.text and auto-placed point labels with ds.labels.
---

import Example from '../../../components/Example.astro';

Both return native Altair layers you compose with `+`, sharing the base chart's scales.

## Text

`ds.text()` places annotations at data coordinates (numbers share the quantitative scale, strings
snap to categories) or at frame positions via nine presets (`topLeft`, `middleCenter`, …). Add a
background chip behind the text with `fill` (`True` for a darkmode-aware grey, or a color),
`fillOpacity`, `stroke`, and `cornerRadius` - useful for keeping the text legible over a busy
field. The chip is drawn only when `fill` is set.

<Example name="text" alt="Scatter plot with a frame-anchored sample-size annotation" />

## Point labels

`ds.labels()` auto-places non-overlapping text labels with connector lines for a set of points -
volcano gene labels, rank-order labels, cluster labels. Placement is deterministic (no RNG, so
figures are reproducible): each label takes the nearest clear spot, and a thin leader line runs
from each point to its label. Pass the full plotted `data` and let `subset=` select which points to
name - an integer picks that many spread evenly across the plot, a list names rows by their
`labels` column value, and a boolean mask (`subset=df["is_hit"]`) selects rows positionally when the
label column repeats. Passing the full `df` also lets the labels dodge every plotted point, not
just the ones being named, so they settle into open space instead of on top of the crowd. Style
the labels with `fontSize`, `color`, and `fontStyle` (e.g. `fontStyle="italic"` for gene or
species names), and give them a background chip with `fill` / `fillOpacity` / `stroke` /
`cornerRadius` (drawn only when `fill` is set) to keep them readable over a dense scatter.

Set `connectorCap="arrow"` to point each connector toward its target. The existing `connectorGap`
controls target clearance and is applied once; label placement and text-end clearance do not change.
Arrowheads appear in `ds.save()` SVG/PNG and `ds.show()`, not raw Altair or interactive HTML, so use
an export for the decorated result:

```python
arrow_labels = points + ds.labels(data, "x", "y", "name", connectorCap="arrow")
ds.save(arrow_labels, "arrow-labels", format=["svg", "png"])
```

<Example name="labels" alt="Scatter plot with eight auto-placed, connector-linked point labels" />

## Superscripts and subscripts

Any label - an axis title, a `ds.text` annotation, a legend title - can carry a superscript or
subscript. It is typeset when you export with `ds.save()` (and on this site's live charts): the run
becomes a shrunk, shifted plain-ASCII glyph, so it looks right even in fonts that lack the Unicode
super/subscript characters. Write a superscript with a caret, and a subscript with a **double**
underscore:

- `x^2`, `10^3` -> x squared, ten cubed
- `C__t`, `x__1` -> C-sub-t, x-sub-1

The run is the one or two characters right after the token. Literal Unicode subscripts you type
yourself are handled too (`H₂O`, `t₀`).

<Example name="subscripts" alt="Concentration-decay line with a C-subscript-t axis title and a squared-superscript annotation" />

The subscript token is a **double** underscore on purpose. A single underscore is the usual
snake_case column-name separator, so a default axis title taken from a column like `x_1` or
`flipper_length_mm` would be mangled if a lone `_` meant "subscript". With `__`, only a deliberate
`C__t` is lowered; ordinary column names - single-underscore or double-underscore (`model__alpha`) -
are left untouched. The caret needs no such guard, since `^` never appears in column names.
